export interface Info {
    name: string;
    capacity: Number;
}

export interface House {
    id: number;
    name: string;
    images: string[];
}

export interface Filter {
    name: string;
    value: any;
}