export interface UserDetails {
    username: string;
    password: string;
    country: string;
    name: string;
    lastName: string;
}

export interface Credentials {
    username: string;
    password: string;
}